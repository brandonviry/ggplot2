---
title: "Projet GGPLOT2"
author: "38001470 VIRY Brandon &  40007818 Chahalane Bériche"
date: '2022-11-21'
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# GUIDE UTILISATION GGPLOT 2

**SOMMAIRE**

-   Introduction (1)

-   Prerequis (2)

-   Syntax de base

    -   Recuperation de données (2)
    -   Les Géometries (2)
    -   Les Mapages (2)
    -   Attribut de represnetation (1)
    -   Multigraphe (1)
    -   Faceting (1)
    -   Scales (2)
    -   Theme (1)

-   Exemple :

    -   Exemple 1 (1)

    -   Exemple 2 (2)

-   Aller Plus loin

    -   Resource (1)
    -   Aide (2)

-   Conclusion (1)

**(1) Bériche** **(2) Brandon**

# Introduction

# **Prérequis**

Pour pouvoir utiliser ce guide **il faut maîtriser le langage de programmation R** il faut avoir installé sur sa machine **r studio** installer La librairie  **ggplot2** et savoir **le type de représentation** qu'on veut faire sur vous donner .Si vous avez jamais installer la librairie est jamais utilisé R studio je vais vous Mettre en annexe un tuto expliquant comment installer un studio et je veux vous expliquer comment installer la librairie ggplot2.

Alors voici comment il faut installer la librairie ggplot2 : 

Etape 1 : Etape de départ

![](installeGGPLOT2/Etape%201%20_%20Etape%20de%20d%C3%A9part.png)

Etape 2: Cliquer sur l'onglet "Pakages"

![](installeGGPLOT2/Etape%202_%20Cliquer%20sur%20l_onglet%20_Pakages_.png)

Etape 3 : Sous-onglet "Install"

![](installeGGPLOT2/Etape%203%20_%20Sous-onglet%20_Install_.png)

Etape 4 : Parametrage

![](installeGGPLOT2/Etape%204%20_%20Parametrage%20.png)

Etape 5 : Install from "CRAN"
![](installeGGPLOT2/Etape%205%20_%20Install%20from%20_CRAN_.png)

Etape 6 : Entrer le nom de la librairie dans "Package" -\> ggplot2

![](installeGGPLOT2/Etape%206%20_%20Entrer%20le%20nom%20de%20la%20librairie%20dans%20_Package_%20-_%20ggplot2.png)

Etape 7 : cocher "install dependencies"

![](installeGGPLOT2/Etape%207%20_%20cocher%20_install%20dependencies_.png)
Etape 8 : Cliquée sur "install"

![](installeGGPLOT2/Etape%208%20_%20Cliqu%C3%A9e%20sur%20_install_.png)

Etape 9 : verifer que le installation est faite voir en bleu sur la photo

![](installeGGPLOT2/Etape%209%20_%20verifer%20que%20le%20installation%20est%20faite%20voir%20en%20bleu%20sur%20la%20photo.png)

# Syntax de base

## Recuperation de données

Pour récupérer les données la plupart du temps vue le nombre de données , les données sont enregistrés   **dans des fichiers**  et le plus souvent ce fichier est dans le format csv  donc je vais vous montrer comment récupérer les données  dans  plusieur type de fichier :

**1- simple fichier texte**

```{r}
data <- read.table("data.txt", header = FALSE)
data 
```

**2- un fichier CSV**

```{r}
 base = read.csv(file = "tic-tac-toe.csv",header = FALSE)
 base
```

**3 - package**

\

Il existe plein d'autres façons aussi de récupérer des données comme l'importation  de **fichier excel ,sas ,spss  et plein d'autres** formats ou encore en faisant **une connexion à des bases de données** .

Maintenant que les données sont récupérées je vais vous expliquer La nature qui contient ces données.C'est donner sont d'un type spécial a R **c'est un** **dataframe** où les **données** de chaque colonnes sont **typées**.

```{r}

#matrice 
donner = matrix(data = c(1,"lapin","noir",2,'chien',0,3,"chien","rouge") , nrow = 3, ncol = 3 , byrow = TRUE)
donner

#dataframe
as.data.frame(donner)


```

pour pouvoir faire la représentation Vous devez d'abord importer la librairie que vous avez installé ggplot2 comme suit:

```{r}
library(ggplot2)
```

pouvoir travailler sur c'est donner vous devais prendre c'est donner est le placer en argument de la fonction ggplot comme ceci  :

<https://www.offremedia.com/le-temps-consacre-aux-jeux-video-est-estime-7-heures-par-semaine-dans-le-monde-et-en-france-en>

```{r}
donnerRp = read.csv(file = "donnerRp.csv",header = TRUE)
donnerRp

colnames(donnerRp)
rownames(donnerRp)
```

```{r}
ggplot(data= donnerRp)
```

ou

```{r}
ggplot(donnerRp) 
```

## Les Géometries

Les géométries c'est le type de représentation graphique qu' on veut utiliser  il existe beaucoup  de géométrie  chaque géométrie a c'est caractéristique . 

Pour choisir le type de représentation il suffit d' ajouter un type de graphique   il son sous forme de fonction comme ceci :

```{r}
ggplot(data=donnerRp)  + geom_point () 
#initialisation donner     addition  geom 

```

Comme vous le voyer les géométries ne peut pas etre représenter sans un argument .

Les géométriques on peut mettre en argument ce qu' on appelle un mappage  (que je vous parlerai Plus tard)qui est défini dans la fonction aes() c'est fonction permette de sélectionner les éléments à utiliser pour la représentation selon les caractéristique d'un type de graphique  comme pour une graphique geom_point():

```{r}
ggplot(data=donnerRp)  + geom_point (aes(x=Heures.moyennes.chaque.semaine , y=Pays )) 
```

aes() mappage

x c'est l'élément du graphique en x

y c'est l'élément du graphique en y 

data1 est la donner pris en compte en x

et 

data2 est la donner pris en compte en y 

Chaque géométrie  possède d'autres arguments qu'on appelle attribut graphique e  (Que je vous parlerai Plus tard)

il existe beaucoup de  géométrique  je vais te présenter quelqu' un

## geom_boxplot : 

```{r}
note =read.csv(file = "note.csv",header = TRUE)
note 
```

```{r}
ggplot(note) + geom_boxplot(aes(x=ECTS , y=Note ))
```

## geom_violin

```{r}
ggplot(note) + geom_violin(aes(x=ECTS , y=Note ))
```

\

## geom_bar 

```{r}

ggplot(note) + geom_bar(aes(x=Note  ))
```

\

## geom_col

```{r}
ggplot(note) + geom_col(aes(x=Note ,y=COUR ))
```

\

## geom_text

```{r}
ggplot(data=note)  + geom_text (aes(x=Note ,y=COUR, label=ECTS )) 
```

\

## geom_label

```{r}
ggplot(data=note)  + geom_label (aes(x=Note ,y=COUR, label=ECTS )) 
```

\

## geom_density

<https://ggplot2.tidyverse.org/reference/geom_density.html>\
<https://plotly.com/ggplot2/geom_density/>

<https://www.datanovia.com/en/lessons/ggplot-density-plot/>

<https://fr.investing.com/currencies/eur-usd-historical-data>

```{r}
EUR_USD=read.csv(file = "EURUSD.csv",header = TRUE)
EUR_USD 

```

```{r}
colnames(EUR_USD)
```

```{r}
ggplot(EUR_USD)+geom_density(aes(x=Variation))
```

## geom_line 

```{r}
ggplot(EUR_USD)+geom_line(aes(x=Variation,y=1:length(rownames(EUR_USD))))
```

\

## geom_point

```{r}
ggplot(note)+geom_point(aes(y=COUR,x=Note))
```

\

## geom_hex

<https://ggplot2.tidyverse.org/reference/geom_hex.html>

<https://plotly.com/ggplot2/geom_hex/>

<https://search.r-project.org/CRAN/refmans/ggplot2/html/geom_hex.html>

<https://tidyverse.github.io/ggplot2-docs/reference/geom_hex.html>

<https://thinkr.fr/pdf/ggplot2-french-cheatsheet.pdf>

```{r}
ggplot(note)+geom_hex(aes(y=COUR,x=Note),bins=0.1)
```

<https://sebastienbuczinski.netlify.app/post/r-2-ggplot/>\

## geom_bin2d

Il  en existe d'autres mais voici les principales utiliser

## Les Mapages

Un mappages permet de relier un élément graphique à une variable d'un tableau Pour chaque géométrie il existe à ma page différent que je vais vous détailler suivant :

## geom_boxplot

-   x =a variable qualitative  

-   y = variable numérique

-   argument :

    -   with : largeur des box

    -   notch : crée un boxplot avec notch si TRUE

    -   color :couleur

    -   size : taille

    -   linetype : type de bordure

        -   \
            \
            <https://www.datanovia.com/en/fr/lessons/ggplot-boxplot/>

## geom_violin

color : Couleur

Size : taille

linetype : type de la ligne

fill: couleur de remplissage

trim : TRUE coupe la queue du voilon et FALSE l'inverse

<https://www.datanovia.com/en/fr/lessons/ggplot-violin-plot/>

## geom_bar 

<https://ggplot2.tidyverse.org/reference/geom_bar.html>

-   x  ou y = la variable qualitative

-   x si on veut représenter verticalement 

-   y si on veut représenter horizontalement

Vous pouvez aussi passer du format vertical horizontal et vice-versa en ajoutant la fonction coord_flip()   à la géométrie :

```{r}
geom_bar(.....)+coord_flip()

```

RQ : coord_flip() que s'appliquer à toute les type de représentation de ggplot 2 

## geom_col

-   x 

-   y 

## geom_text

-   x  la position des étiquettes en x

-    y la position des étiquettes en y 

-    label  : le texte a afficher

    ## geom_label

-   x  la position des étiquettes en x

-    y la position des étiquettes en y 

-    label  : le texte à afficher 

RQ: geom_text  et geom_label sont identique sauf dans l'affiche du texte 

\

## geom_density

-   x    

-   y

-   color 

-   size 

\

## geom_line 

-   x

-   y

## geom_point

-   x

-   y

\

## geom_hexa 

-   x

-   y

## geom_bin2d

-   x

-   y\

RQ : geom_point, geom_hexa ,geom_bin2d fonctionne pareil la seul différence c'est la forme géométrique de la représentation : geom_point c'est un point ,geom_hexa  c'est un hexagone ,geom_bin2d un polygone à 4 côtés 

\

Il en existe peut-être d'autres mais ce sont les plus importants 

## Attribut de represnetation

## Multigraphe

## Faceting

## Scales

Permet de modifier la façon de relier des attribut graphique et les mappage  pour la représentation .On peut  le mettre à une représentation graphique en additionnent  une des fonction de scale a la géométrie :

```{r}
ggplot(data=donnerRp)  + geom_point (aes(x=data1 , y=data2 ),color = "darkgreen", size = 3, alpha = 0.3
)
 scale_x_continuous(limits = c(0,100)) 

```

## **scale_size**

Alors la première fonction scal que je vais vous présenter permet de modifier la taille  des éléments  a représenté c'est fonction se nomme scale_size ces fonction modifier minimales et maximales  de la taille de l'objet sur un mappage size cette fonction prend en argument un argument range du un vecteur de taille 2 avec le min et le max 

```{r}
scale_size(range = c(0, 100))
```

Où le 0 c'est le minimum et le sang le maximum 

\

## **scale_x.... et scale_y...**

\

Les prochaines fonctions sont scale qui modifie sur les X et Y 

\

Les fonctions du genre scale_x.... ou scale_y...  Modifié le l'axe x et y du graphique   cette fonction prend en argument un argument limite qui est un vecteur de taille 2 avec le min et le max . 

\

Voici quelques fonction : 

\

-   scale_x\_continuous 

-   scale_y\_continuous

-   scale_x\_log10 

-   scale_y\_log10 

-   scale_x\_discrete 

-   scale_y\_discrete 

\

## **scale_color**

Mais il existe un autre type de scal qui permet de modifier la couleur ses fonctions sont du genre scale_color ...   Kimmodifier les palettes de couleur  loque on on utilise le attribut graphque  color .

\

 Voici une liste de fonctions scale_color : \

-   scale_color_gradient qui prend en argument deux paramètre low et high ou low et higt sont une couleur en string ,low falleur la plus faible et high valeurs en plus fort

-   scale_color_viridis_c : ajouter un titre a la legende coleure 

-    scale_fill :  modifier les palettes de couleur  loque on on utilise le attribut graphqUE fill

## Theme

# Exemple :

## Exemple 1

## Exemple 2

# Aller Plus loin

## Resource

## Aide

# Conclusion

chien
